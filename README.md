# Digital-Marketing-Performance-Analysis-SQL
 A 5-table relational schema built from weekly marketing and booking data, queried with multi-table joins, CTEs, and window functions (RANK, DENSE_RANK, LAG, rolling averages) to isolate exactly where a booking funnel breaks down.
